apiVersion: v1
kind: Pod
metadata:
  creationTimestamp: null
  labels:
    run: stress-test
  name: stress-test
  namespace: team-alpha
spec:
  containers:
  - image: busybox
    name: stress-test
    command:
    - sh
    - -c
    - |
      while true
      do
        wget -qO- http://alpha-api-svc
      done
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
