apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: alpha-tls
  namespace: team-alpha
spec:
  secretName: alpha-tls-secret
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
  dnsNames:
  - frontend.local
  - api.local