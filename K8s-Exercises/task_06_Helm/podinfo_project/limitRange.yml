apiVersion: v1
kind: LimitRange
metadata:
  name: team-beta-limit-range
  namespace: demo
spec:
  limits:
  - default:
      cpu: 500m
      memory: 256Mi
    defaultRequest:
      cpu: 100m
      memory: 128Mi
    max:
      cpu: "1"
    min:
      cpu: 50m
    type: Container
