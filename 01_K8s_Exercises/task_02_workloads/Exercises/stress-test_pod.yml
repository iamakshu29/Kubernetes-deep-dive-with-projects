apiVersion: v1
kind: Pod
metadata:
  creationTimestamp: null
  labels:
    run: stress-test
  name: stress-test
  namespace: team-alpha
spec:
  containers:
  - image: busybox
    name: stress-test
    command:
    - sh
    - -c
    - |
      while true
      do
        wget -qO- http://alpha-api-svc >/dev/null && echo "OK" || echo "FAILED"
        sleep 0.1
      done
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
