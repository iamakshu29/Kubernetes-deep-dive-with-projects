# Regarding Tasks
- Unable to perform all the tasks as they will need a separate repo just for gitOps. I am not creating it.\
- Main Go through each task and understand it conceptually.

# Hooks
The Hooks in `Exercise_06` work only if they are in the gitOps repo
i.e. where the app is present. 
- as per the argocd-app.yml. The repo is
- `https://github.com/iamakshu29/devops-cicd-toolchain.git`