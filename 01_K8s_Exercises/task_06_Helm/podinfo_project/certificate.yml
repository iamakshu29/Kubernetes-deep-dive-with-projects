apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: podinfo-app-tls
  namespace: demo
spec:
  secretName: podinfo-app-tls-secret
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
  dnsNames:
    - podinfo.local