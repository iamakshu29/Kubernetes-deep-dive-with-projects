apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ .Values.backend.podName }}-pdb
spec:
  minAvailable: {{ .Values.backend.pdb.minAvailable }}
  selector:
    matchLabels:
      app: {{ .Values.backend.podLabels }}
